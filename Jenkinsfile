pipeline {
    agent any

    stages {
        stage('Checkout'){
            ...
        }

        stage('Git Version'){
            ...
        }

        stage('Docker Version'){
            ...
        }

        stage('Docker Build'){
            ...
        }

        stage('Docker Images'){
            ...
        }
    }
}
